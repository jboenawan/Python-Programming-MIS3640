import math 

def mysqrt(a)
    while True:
        print(x)
        y=(x+a/x)/2
        if y == x:
            break
        x=y

def math(n):
    n = float(n)
    return math.sqrt(a)

def test_square_root():
    diff = mysqrt - math 
    print "{:<12}\t {:<12}\t{}".format(mysqrt, math, diff)
    for i in range(1,10):
        m = mysqrt(i,x)
        ma = math(i)
        ab = abs(n-1)
        print "{:<12}\t{:<12}\t{}".format(m, ma, ab)

test_square_root()
